<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TransitInformation" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary_foreground_dark</item>
        <item name="colorPrimaryVariant">?attr/colorPrimary</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">?attr/colorPrimaryVariant</item>
        <item name="actionBarStyle">@style/DarkModeActionBar</item>
        <!-- Custom attrs -->
        <item name="defAppBackground">@style/defAppDarkBackground</item>
        <item name="defAppForeground">@style/defAppDarkForeground</item>
    </style>

    <!-- DarkMode(ActionBar) -->
    <style name="DarkModeActionBar" parent="Widget.MaterialComponents.ActionBar.Solid">
        <!-- Primary brand color. -->
        <item name="background">?attr/colorPrimaryVariant</item>
    </style>
</resources>